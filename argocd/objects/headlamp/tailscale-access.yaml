---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: headlamp-tailscale
spec:
  defaultBackend:
    service:
      name: headlamp
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - headlamp
